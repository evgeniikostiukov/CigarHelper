# TODO — задачи репозитория

Файл для **открытых** задач. Выполненное: `- [ ]` → `- [x]` или блок **«Сделано»** внизу.

---

## Нетестовые задачи (продукт и техдолг)

- [ ] Периодически обновлять зависимости (`npm audit`, Browserslist) и устранять критичные уязвимости без ломки сборки.
- [ ] Разобраться с предупреждением Tailwind при `vite build` (`unknown utility class`, при необходимости `@reference` / конфиг v4).
- [ ] Проверить предупреждение NuGet `NU1510` для `Microsoft.Extensions.Configuration.Json` в `CigarHelper.Api`.
- [ ] По мере роста бандла — точечный code-splitting тяжёлых экранов (например формы с редактором).
- [ ] Актуализировать `docs/memory-bank/` после крупных изменений API, маршрутов или обязательных секретов.

---

## Этап 0 — База и договорённости (CI, скрипты)

- [x] Зафиксировать в CI минимальную проверку фронта: `npm run build` и при наличии скрипта — `npm run lint` / `vue-tsc` (команды из `CigarHelper.Web`).
- [x] Убедиться, что `dotnet test CigarHelper.sln` стабильно проходит в CI на чистой машине (окружение Testing, без реальной БД).

## Этап 1 — Бэкенд (расширение текущего покрытия)

- [x] Просмотреть критичные контроллеры без интеграционных тестов; добавить тесты там, где риск выше (авторизация, границы владения, публичные vs приватные эндпоинты).
- [x] Добавить/уточнить интеграционные тесты для сценариев, от которых сильно зависит SPA (пагинация списков, ошибки 401/403/404, типичные DTO).
- [ ] При изменении контрактов API — обновлять или добавлять тесты в `CigarHelper.Api.Tests` до или вместе с правкой фронта.

## Этап 2 — Фронт: юнит-тесты (Vitest)

- [x] Подключить Vitest к `CigarHelper.Web` (конфиг + скрипт `npm test` / `npm run test:unit`).
- [x] Покрыть тестами утилиты без DOM (`src/utils/`, например преобразования изображений, форматирование), где логика нетривиальна.
- [ ] По мере выноса логики из крупных `.vue` — добавлять точечные тесты на чистые функции/composables.

## Этап 3 — Фронт: компонентные тесты (выборочно)

- [ ] Подключить Vue Test Utils совместно с Vitest (если ещё не сделано на этапе 2).
- [ ] Написать тесты для 1–2 переиспользуемых или сложных компонентов (диалоги, нестандартная логика без полного E2E).

## Этап 4 — E2E (Playwright)

- [ ] Добавить Playwright в репозиторий (отдельная папка `e2e/` или внутри `CigarHelper.Web` — по принятому в команде решению).
- [ ] Описать в README/memory bank, как поднимать API + фронт для локального прогона E2E (порты, тестовый пользователь, env).
- [ ] Реализовать smoke-набор: логин, открытие 2–3 ключевых разделов, один сценарий с диалогом/таблицей (например база сигар).
- [ ] Подключить прогон E2E в CI (по возможности с поднятием API в Testing или против staging).

## Этап 5 — По необходимости (не в первую очередь)

- [ ] Визуальная регрессия (скриншоты) — если появится жёсткая дизайн-система или частые визуальные регрессии.
- [ ] Контрактные тесты фронт↔бэк (Pact и т.п.) — если интеграционных тестов API станет недостаточно для независимых команд.
- [ ] Сценарии доступности (axe в Playwright) для публичных страниц.

---

## Сделано

- [x] **2026-03-30** — Зависимости: `npx npm-check-updates -u` в `CigarHelper.Web`, корне репо и `cigar-scraper` + `npm install`; TypeScript зафиксирован на **^5.9.3** (peer `typescript-eslint@8` требует TS ниже 6); TipTap 3 в `TextEditor.vue` (`Extensions`, `setContent(..., { emitUpdate: false })`, стили тулбара без `@apply` под Tailwind 4.2); удалён `@types/dompurify`.
- [x] **2026-03-30** — Фронт: палитра светлее и с розово-красным акцентом — пресет PrimeVue Aura (`rose` primary, `surface` на stone), градиенты/акценты Tailwind `amber`→`rose`, фоны `stone-100`→`stone-50` на корневых экранах, тёплый цвет `body` в `main.css`, hover/focus меню в `App.vue`.
- [x] **2026-03-30** — Этап 1 бэкенд-тестов: `ApiAuthorizationAndContractsIntegrationTests` (401/403/404, публичный профиль, пагинация `PaginatedResult`), исправление InMemory в `Program.cs` (одно имя БД на запуск хоста — сиды видны HTTP).
- [x] **2026-03-30** — CI GitHub Actions: `dotnet test` + фронт (`typecheck`, `lint`, `test`, `build`). Скрипты в `CigarHelper.Web/package.json`, `eslint-config-prettier`, удалён устаревший `.eslintignore`.
- [x] **2026-03-30** — Исправления под `vue-tsc`: тип `User.role` (readonly из JWT), `getRoleClaims`, `getAuthUserId`; автоформат ESLint/Prettier по проекту; мелкие правки линта (emit tuple, кнопка «Создать похожую» в `CigarDetailDialog`, вызов `addCigarToHumidor` без лишнего аргумента).
- [x] **2026-03-30** — Vitest + `happy-dom`: `vitest.config.ts`, тесты `imageUtils.test.ts`, `roles.test.ts`.

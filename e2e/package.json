{
  "name": "cigar-helper-e2e",
  "private": true,
  "version": "1.0.0",
  "description": "Playwright E2E для CigarHelper (API + SPA поднимаются вручную, см. README)",
  "engines": {
    "node": ">=24.14.1 <25"
  },
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0"
  }
}
